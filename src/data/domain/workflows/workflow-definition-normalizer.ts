import type { WorkflowDefinition, WorkflowNodeData } from "@/data/domain/models"

function normalizeNode(data: Partial<WorkflowNodeData>, fallback: Pick<WorkflowNodeData, "kind" | "label" | "prompt">): WorkflowNodeData {
  return { ...data, label: data.label ?? fallback.label, prompt: data.prompt ?? fallback.prompt, kind: data.kind ?? fallback.kind, agentId: data.agentId ?? "", task: data.task ?? data.prompt ?? fallback.prompt, description: data.description ?? "", enabled: data.enabled ?? true, continueOnError: data.continueOnError ?? fallback.kind === "start", retryCount: data.retryCount ?? 0, timeoutMs: data.timeoutMs ?? 30000, modelId: data.modelId ?? "", runIf: data.runIf ?? "", inputTemplate: data.inputTemplate ?? "", outputKey: data.outputKey ?? "", maxInputChars: data.maxInputChars ?? 8000, maxOutputChars: data.maxOutputChars ?? 8000, documentId: data.documentId ?? "", documentName: data.documentName ?? "", queryExpression: data.queryExpression ?? "$input.query", resultLimit: data.resultLimit ?? 5, integrationId: data.integrationId ?? "", integrationName: data.integrationName ?? "", method: data.method ?? "POST", url: data.url ?? "", headersJson: data.headersJson ?? "{}", queryJson: data.queryJson ?? "{}", bodyJson: data.bodyJson ?? "{}", runtime: data.runtime ?? "node", script: data.script ?? "export async function main(input) {\n  return { ok: true, input }\n}\n", timeoutSeconds: data.timeoutSeconds ?? 60, branchCount: data.branchCount ?? 2, joinStrategy: data.joinStrategy ?? "wait-all", trueLabel: data.trueLabel ?? "True", falseLabel: data.falseLabel ?? "False", branches: data.branches ?? [{ id: `${fallback.label}-true`, label: "True", expression: "$input.ok === true" }, { id: `${fallback.label}-false`, label: "False", expression: "" }], variableName: data.variableName ?? "", variableValue: data.variableValue ?? "", template: data.template ?? "", templateOutputFormat: data.templateOutputFormat ?? "text", loopExpression: data.loopExpression ?? "$input.items", maxIterations: data.maxIterations ?? 25, emailTo: data.emailTo ?? "", emailSubject: data.emailSubject ?? "", emailBody: data.emailBody ?? "", systemPrompt: data.systemPrompt ?? "", temperature: data.temperature ?? 0.7, maxTokens: data.maxTokens ?? 1024, responseFormat: data.responseFormat ?? "text", inputSchemaJson: data.inputSchemaJson ?? "{}", outputSchemaJson: data.outputSchemaJson ?? "{}", itemAlias: data.itemAlias ?? "item", concurrency: data.concurrency ?? 2, mergeStrategy: data.mergeStrategy ?? "all-settled", notes: data.notes ?? "" }
}
export function normalizeWorkflowDefinition(definition: WorkflowDefinition): WorkflowDefinition { return { ...definition, nodes: definition.nodes.map((node, index) => ({ ...node, type: node.type ?? "workflowNode", data: normalizeNode(node.data, { kind: node.data.kind ?? "agent", label: node.data.label ?? `Step ${index + 1}`, prompt: node.data.prompt ?? "Describe what this node should do." }) })), edges: definition.edges.map((edge) => ({ ...edge, data: { condition: edge.data?.condition ?? "", successOnly: edge.data?.successOnly ?? false } })), viewport: definition.viewport ?? { x: 0, y: 0, zoom: 1 }, resourceBindings: definition.resourceBindings ?? { providerId: "provider-openai", skillId: "skill-plan", documentId: "document-product-manual", integrationId: "integration-webhook" }, dryRunInputJson: definition.dryRunInputJson ?? '{\n  "leadId": "LD-1001"\n}', variables: definition.variables ?? [], budget: definition.budget ?? { maxSteps: 8, maxDurationMs: 120000 } } }
